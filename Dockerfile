# Используйте официальный образ Node.js
FROM node:18-alpine

# Создайте директорию для вашего приложения в контейнере
WORKDIR /usr/src/app

# Копируйте файлы проекта в контейнер
COPY . .

# Установите зависимости
RUN npm install

# Запуск приложения
CMD ["npm", "run", "server"]
